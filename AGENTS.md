# APoW CLI Codex Entry

Apply `/Users/aklo/projects/apow/AGENTS.md` first.

## Scope
- This repo owns the `apow-cli` TypeScript package, mining loop, wallet flows, native grinder build wiring, dashboard, and APoW skill source.
- `skill.md` is canonical here; sync it to the website, GitBook docs, and Base skill copy when intentionally changed.
- Verify package versioning and publish identity before any npm publish.
- Keep work inside `apow-cli` unless the user asks for cross-repo APoW synchronization.
- Do not create local `.codex/AGENTS.md`, `.codex/CLAUDE.md`, or external project routing files.
